Start a new SENA 🦁 collaboration session.

IMPORTANT: Use `sena` from PATH, NOT ./target/release/sena

Execute: `sena join --role "$ARGUMENTS" --name "SENA-Claude"`

This joins the SENA collaboration hub for multi-session teamwork.
