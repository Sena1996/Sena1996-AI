Format data as a SENA 🦁 styled table.

IMPORTANT: Use `sena` from PATH, NOT ./target/release/sena

Use: `sena format table --title "Title" '<json-data>'`

The table will use Unicode box-drawing characters in SENA style.
