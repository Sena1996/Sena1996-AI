SENA 🦁 Peer Management

IMPORTANT: Use `sena` from PATH, NOT ./target/release/sena

Manage network peers:
- List: `sena peer list`
- Add: `sena peer add <ip> --name "Peer Name"`
- Authorize: `sena peer authorize <id>`
- Connect: `sena peer connect <ip> --token <token>`
